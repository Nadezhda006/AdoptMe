@model IEnumerable<AdoptMe.DTOs.PetDTO>

@{
    ViewData["Title"] = "Pets";
}

<link rel="stylesheet" href="~/css/styles.css" />

<header>
    <h1>Pets</h1>
</header>
<p>
    @if (User.IsInRole("Admin"))
    {
        <a asp-action="Create" class="btn-primary">Add new pet</a>
    }
    else
    {

    }
</p>

<div class="container">
    <div class="cat-list">
        @foreach (var item in Model)
        {
            <div class="cat-card">

                <a asp-action="Details" asp-route-id="@item.Id">
                    <img src="@item.ImageURL" alt="@item.Name">
                    @* <img src="@Html.DisplayFor(modelItem => item.ImageUrl)" alt="Image" style=" max-width: 250px; max-height: 300px;" /> *@
                </a>
                <h3>@item.Name</h3>
                <p>Breed: @item.Breed</p>
                <p>Age: @item.Age years old</p>
                <div class="btn-group">

                    @if (User.IsInRole("Admin"))
                    {

                        <a asp-action="Details" asp-route-id="@item.Id" class="btn-details">Details</a>

                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn-edit">Edit</a>

                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn-delete">Delete</a>

                    }
                    else
                    {

                    }

                </div>
            </div>
        }
    </div>
</div>
